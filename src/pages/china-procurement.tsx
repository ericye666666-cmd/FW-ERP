export function ChinaProcurementPage() {
  return (
    <section className="rounded-3xl border border-[color:var(--panel-border)] bg-[var(--panel-bg)] p-8 shadow-sm">
      <h2 className="text-2xl font-semibold tracking-tight">中方管理</h2>
      <p className="mt-3 text-sm leading-7 text-[color:var(--muted)]">中方采购管理总览入口（迁移自仓库功能区）。</p>
    </section>
  );
}
